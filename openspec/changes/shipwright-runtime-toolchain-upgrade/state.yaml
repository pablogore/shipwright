# SDD DAG state for change: shipwright-runtime-toolchain-upgrade
change: shipwright-runtime-toolchain-upgrade
title: Provider-Managed Runtime/Toolchain Upgrade (Go First)
updated: 2026-08-29 (slice 3 verify)

session:
  execution_mode: auto
  artifact_store: openspec
  delivery_strategy: auto-chain
  chain_strategy: stacked-to-main
  review_budget_lines: 400

phases:
  explore: done
  propose: done
  spec: done
  design: done
  tasks: done
  apply: partial (slice 3/4 applied -- tasks 1.1-1.24, 2.1-2.17, and 3.1-3.6 done, phase 4 not started)
  verify: partial (slice 3/4 verified -- PASS WITH WARNINGS; slice 4 not yet applied/verified)
  archive: pending

artifacts:
  exploration: openspec/changes/shipwright-runtime-toolchain-upgrade/exploration.md
  proposal: openspec/changes/shipwright-runtime-toolchain-upgrade/proposal.md
  proposal_es: openspec/changes/shipwright-runtime-toolchain-upgrade/proposal.es.md
  spec_runtime_toolchain: openspec/changes/shipwright-runtime-toolchain-upgrade/specs/runtime-toolchain/spec.md
  spec_runtime_toolchain_es: openspec/changes/shipwright-runtime-toolchain-upgrade/specs/runtime-toolchain/spec.es.md
  spec_workflow_manifest: openspec/changes/shipwright-runtime-toolchain-upgrade/specs/workflow-manifest/spec.md
  spec_workflow_manifest_es: openspec/changes/shipwright-runtime-toolchain-upgrade/specs/workflow-manifest/spec.es.md
  spec_public_module_api: openspec/changes/shipwright-runtime-toolchain-upgrade/specs/public-module-api/spec.md
  spec_public_module_api_es: openspec/changes/shipwright-runtime-toolchain-upgrade/specs/public-module-api/spec.es.md
  design: openspec/changes/shipwright-runtime-toolchain-upgrade/design.md
  design_es: openspec/changes/shipwright-runtime-toolchain-upgrade/design.es.md
  tasks: openspec/changes/shipwright-runtime-toolchain-upgrade/tasks.md
  tasks_es: openspec/changes/shipwright-runtime-toolchain-upgrade/tasks.es.md

decisions:
  D1:
    topic: manual/approval gating
    resolution: operator-invoked trigger; no blocking primitive added to engine/execute.go
    spec_impact: none — workflow-execution approval-gate requirement preserved verbatim
  D2:
    topic: SCM/PR adapter
    resolution: fully out of scope; deferred to a follow-up change
    spec_impact: none — capability returns a mutated directory, no remote-write path exists

next_recommended:
  - open PR #1 (feature/runtime-inspect-go -> develop) -- slice 1 verify PASSed
  - open PR #2 (feature/runtime-upgrade-single-module -> feature/runtime-inspect-go) -- slice 2 verify PASS WITH WARNINGS
  - open PR #3 (feature/runtime-upgrade-workspace -> feature/runtime-upgrade-single-module) -- slice 3 verify PASS WITH WARNINGS
  - sdd-apply (slice 4, phase 4 of tasks.md, after PR #3 merges to develop)

open_risks:
  - VERIFIED (slice 1/4, 2026-08-28) -- runtime-inspect end-to-end on
    feature/runtime-inspect-go PASSes: all 24 Phase-1 tasks confirmed against
    real code (not just checkmarks); spec.md runtime-toolchain scenarios
    (zero-mutation, ambiguous-reported-not-guessed, missing-location-omitted,
    failOnDrift fail-closed) each covered by a passing runtime test; design.md
    D-1/D-2/D-3/D-4/D-4b/D-8/D-9 decisions match implementation; static
    import-guard (threat_matrix_test.go) plus direct source read confirm no
    network/git/SCM/os.WriteFile/exec.Command path in toolchain.go or
    runtimeinspector.go; api.golden/capabilities_test.go correctly carry 6
    interfaces; no RuntimeUpgrader/mutation code exists yet (correctly
    deferred to Phase 2). go build ./..., go test -race ./... (root +
    providers/go), golangci-lint run ./... (root + providers/go) all
    independently reconfirmed clean (only the 3 known pre-existing
    staticcheck issues in internalimport_test.go/naming_test.go, both
    untouched by this diff). Live --list-steps smoke test against a
    runtime-inspect manifest step succeeded (exit 0). Full report: Engram
    sdd/shipwright-runtime-toolchain-upgrade/verify-report.
  - VERIFIED (slice 2/4, 2026-08-29) -- single-module runtime-upgrade on
    feature/runtime-upgrade-single-module (commit 4e70c8e) PASS WITH WARNINGS:
    all 17 Phase-2 tasks (2.1-2.17) confirmed against real code, not just
    checkmarks. 8/8 in-scope spec scenarios COMPLIANT (Discovery-Driven
    Upgrade x3, No Network/Git/SCM, Versioned Contract surface-count-to-7,
    Composable/Orthogonal independence, workflow-manifest runtime-upgrade
    validates + seven-value allowlist). targetVersion validated against
    modfile.GoVersionRE before any write, proven by a malicious-string test
    table ("1.26.7; rm -rf /", "--flag", "$(whoami)"). D-3 Layer-2 shape
    confirmed by direct read: .dagger/capabilities.go's RuntimeUpgrader.Upgrade
    drops error (matches Builder.Build), unlike Phase 1's RuntimeInspector.
    Inspect which keeps it. go.work-having workspaces are explicitly rejected
    fail-closed (no multi-module traversal loop exists -- correctly deferred
    to Phase 3, tasks 3.1-3.6 unstarted). No go mod tidy/container validation
    code exists yet (correctly deferred to Phase 4). api.golden/
    capabilities_test.go carry 7 interfaces. go build ./..., go test -race
    -count=1 ./... (root + providers/go, uncached), golangci-lint run ./...
    (root + providers/go) independently reconfirmed: build/tests clean, lint
    shows the same 97 (root) + 3 (providers/go) pre-existing issues with zero
    new issues in touched files (line-by-line hunk cross-check). TWO WARNINGS,
    zero CRITICALs: (1) apply-progress lacks the formal per-task TDD Cycle
    Evidence table Strict TDD Mode expects, though RED/GREEN labeling in
    tasks.md itself plus passing tests substantively demonstrate TDD was
    followed; (2) actual diff (5484f1b..4e70c8e, 25 files, 1055+/59-) is
    ~2.4x tasks.md's ~430-line slice-2 forecast, well above the 400-line
    review budget guideline -- PR #2's reviewer should be aware. Full report:
    Engram sdd/shipwright-runtime-toolchain-upgrade/verify-report (upserted,
    slice 2 content).
  - VERIFIED (slice 3/4, 2026-08-29) -- multi-module go.work upgrade on
    feature/runtime-upgrade-workspace (commit 5df1a98) PASS WITH WARNINGS: all
    6 Phase-3 tasks (3.1-3.6) confirmed against real code, not just
    checkmarks. Multi-Module Workspace Consistency (all-or-nothing abort):
    GoRuntimeUpgrader.Upgrade calls detectConflicts (A1/A2/A3) before any
    WithNewFile, confirmed by direct source read (runtimeupgrader.go:95-107)
    -- a conflict anywhere in the workspace aborts the whole call, never a
    per-module skip. A7 path-escape guard (validateModulePaths in
    toolchain.go) runs before gatherWorkspaceInput/any mutation;
    TestUpgrade_PathEscape asserts both CodeA7 and
    mockDir.AssertNotCalled(t, "WithNewFile", ...) -- the stronger
    zero-mutation guarantee the task specifically asked for, not just an
    error return. D2 structural safety preserved: toolchain.go and
    runtimeupgrader.go import only fmt/path/sort/strings/modfile/semver
    (and json/errors/context/dagger in runtimeupgrader.go) -- no os,
    os/exec, net, or git-library import; module-path validation and the
    mutation loop stay pure-Go over already-read bytes/paths. pkg/shipwright/
    runtime.go's UpgradeReport.Modules []ModuleDrift is genuinely populated:
    TestUpgrade_Workspace drives Upgrade() end-to-end over a 3-module mocked
    DaggerDirectory chain and asserts all 3 modules present in the decoded
    JSON report with correct PreviousGo/UpdatedGo. Phase 2 regression:
    single-module behavioral tests (NilClient, NilSource, SingleModule_
    HappyPath, Downgrade_AmbiguousAbort, Malformed_AmbiguousAbort,
    WritesUpgradeReport) are byte-for-byte unmodified in this diff (hunk
    boundaries confirmed) and still pass; only the now-obsolete
    TestGoRuntimeUpgrader_Upgrade_GoWork_NotYetSupported was removed, which
    is the correct, designed consequence of Phase 3 lifting that explicit
    Phase-2 scope boundary. Task 3.5's integration-tagged real-engine test
    (TestGoRuntimeUpgrader_Upgrade_Workspace_RealEngine) was appended to the
    pre-existing testing/integration/go/integration_test.go (relocated
    there in commit 5e396f0, predating this change) -- no new location or
    convention invented. Scope check: diff (511+/47- across 7 files, incl.
    tasks.md/state.yaml) stays inside Phase 3 -- no go mod tidy invocation,
    no From("golang:"...) container chain, no GoSumChanged/AddedModules/
    RemovedModules fields; those remain correctly deferred to Phase 4.
    Independently reconfirmed clean: go build ./... (root + providers/go),
    go test -race ./... (root + providers/go, all green), go test -run
    TestUpgrade_Workspace ./providers/go/... (task 3.6's named check,
    PASS), golangci-lint run ./... (root: 97 pre-existing issues, zero in
    the 6 changed non-test/test files; providers/go: 3 pre-existing
    staticcheck issues in internalimport_test.go/naming_test.go, zero in
    changed files), golangci-lint run --build-tags=integration
    ./testing/integration/go/... (0 issues). ONE WARNING, zero CRITICALs:
    task 3.1's own framing ("wire A1/A2/A3 detectConflicts results... into
    Upgrade's pre-mutation abort path... over workspace-3-modules,
    divergent-go, divergent-toolchain, work-go-mismatch fixtures") implies
    an Upgrade()-level regression test proving zero mutation on those three
    conflict fixtures, matching the pattern TestUpgrade_PathEscape
    established for A7 (mock AssertNotCalled). What was actually delivered
    is TestDetectConflicts_WorkspacePreMutation, which drives
    detectConflicts directly over those four fixtures (correct results,
    including the workspace-3-modules no-conflict case) but never through
    GoRuntimeUpgrader.Upgrade itself. The wiring is verified correct by
    direct source read (Upgrade calls detectConflicts before any
    WithNewFile, unconditionally, for both the single-module and
    workspace paths), and the general Upgrade-aborts-cleanly pattern is
    independently proven at least once end-to-end by both
    TestUpgrade_PathEscape (A7) and Phase 2's still-passing
    Downgrade_AmbiguousAbort/Malformed_AmbiguousAbort (A4/A5) -- so this is
    a test-strength gap, not a functional gap. Recommend an
    Upgrade()-level regression test for A1/A2/A3 (mirroring
    TestUpgrade_PathEscape's AssertNotCalled pattern) either now or folded
    into slice 4's cleanup, but it does not block PR #3. Full report:
    Engram sdd/shipwright-runtime-toolchain-upgrade/verify-report (upserted,
    slice 3 content).
  - NOTE -- phases.apply was still "pending" in state.yaml despite
    apply-progress recording all of Phase 1 (tasks 1.1-1.24) complete;
    corrected to "partial (slice 1/4)" by this verify pass. Phases 2-4 apply
    work remains genuinely unstarted.
  - RESOLVED BY TASKS — 4-slice chain confirmed (feature/runtime-inspect-go,
    feature/runtime-upgrade-single-module, feature/runtime-upgrade-workspace,
    feature/runtime-upgrade-tidy-validate), each cut from and merged to
    `develop` in order (this repo's Git Flow trunk), auto-chain requires no
    further decision before apply. See tasks.md Review Workload Forecast.
  - RESOLVED BY DESIGN — 400-line budget. Design forecasts ~1,650 authored
    lines (4.1x budget). delivery_strategy single-pr is NOT achievable; the
    orchestrator must re-resolve to a chain or grant an explicit
    size:exception. Design recommends a 4-slice chain (see design.md
    "Review Budget Forecast"), not the two-slice chain sdd-spec assumed.
  - Capability allowlist grows 5 -> 7, requiring MODIFIED deltas for
    workflow-manifest and public-module-api.
  - RECONCILED — Go interface names updated to RuntimeInspector and
    RuntimeUpgrader (agent-noun, matching Builder/Tester/Artifactor/
    Deployer/Runner) throughout public-module-api and workflow-manifest
    deltas, both language variants. Capability strings
    runtime-inspect/runtime-upgrade were already correct and unchanged.
  - RECONCILED — public-module-api/spec.md (+ .es.md) now carries a second
    MODIFIED requirement, "Composable, Orthogonal Capabilities", enumerating
    the same seven-value set as the sibling "Versioned Contract Stable"
    delta, with a scenario asserting RuntimeInspector/RuntimeUpgrader are
    independently usable.
  - PROPOSAL GAP found by design — main.go's resolveCapabilityRef is a SIXTH
    core five-branch capability switch (drives --list-steps) missing from
    proposal.md's Affected Areas table.
  - PROPOSAL GAP found by design — providers/go/daggerkit lacks
    Directory.File/Entries/WithNewFile and File.Contents; ~95 unbudgeted
    lines are required before any go.mod can be read or written.
  - LIVE DRIFT is worse than exploration.md recorded: six distinct Go
    versions, including .go-version=1.26.1 and three release*.yml files at
    GO_VERSION 1.26.1, neither of which exploration detected. Design adds
    .go-version as an in-scope tier-1 source (D-4).
  - RECONCILED — specs/runtime-toolchain/spec.md (+ .es.md) validation
    scenario now cites only "go build ./..." per design D-6; the "go vet"
    mention was trimmed.
  - RECONCILED — specs/runtime-toolchain/spec.md (+ .es.md) abort scenario
    now reads "no file in the workspace was mutated — the returned error
    carries no directory, so the caller receives no output to consume",
    matching Upgrade's (nil, err) fail-closed return.
  - REQUIRED task step for sdd-tasks: regenerate pkg/shipwright/testdata/
    api.golden with -update (reviewed diff), and add both new interfaces to
    pkg/shipwright/capabilities_test.go's interface map. CI fails the moment
    the two interfaces compile otherwise.
